# -----------------------------------------------
# properties for "local" configuration
# -----------------------------------------------

# We access directly each micro-service by its port (token_mode=none)
api_hostname=http://localhost
ws_hostname=ws://localhost

# Gateway mode (token_mode=jwt)
#api_hostname=http://localhost/api/gateway
#ws_hostname=wss://localhost/ws/gateway

# 2 token modes:
# - none (then 'username' prop is required)
# - jwt : JWT bearer (then 'bearer' prop is required)
token_mode=none
bearer=
username=bdduser